package ai.masaic.openresponses.api.controller

import org.springframework.context.annotation.Configuration

/**
 * Test configuration class that helps with setting up the test environment.
 * Used for component tests where specific beans need to be configured.
 */
@Configuration
class TestConfiguration 
